[package]
name = "mcp_core"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
jsonschema = "0.21"
schemars = { version = "0.8", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
futures = "0.3"
