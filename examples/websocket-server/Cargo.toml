[package]
name = "mcp-websocket-server"
version = "0.1.0"
edition = "2024"

[dependencies]
mcp_core = { path = "../../core" }
mcp_server = { path = "../../server", features = ["websocket"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
axum = "0.7"
